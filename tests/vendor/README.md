`jsQR.js` — [jsQR](https://github.com/cozmo/jsQR) 1.4.0, Apache-2.0. Test-only: it decodes the QR codes the
self-test rasterizes from the model, as an independent check that they read correctly. It is not used by the app.
